VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_SUPABASE_PUBLISHABLE_KEY=

# --- Mercado Pago ---
# Frontend (publicable)
VITE_MP_PUBLIC_KEY=

# Server-only (NO VITE_ prefix)
MP_ACCESS_TOKEN=

# --- Server-only (NO VITE_ prefix) ---
# Estas llaves jamás deben llegar al frontend. El visor web usa un proxy en el
# dev server de Vite (solo en `npm run dev`) para evitar exponer secretos.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-5-sonnet-latest
GROQ_API_KEY=
GROQ_MODEL=llama-3.1-8b-instant
